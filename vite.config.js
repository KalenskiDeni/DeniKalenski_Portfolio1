import { defineConfig } from 'vite';

export default defineConfig({
  base: '/DeniKalenski_Portfolio1/', // Replace with your GitHub repository name
  build: {
    outDir: 'dist', // Directory where Vite will generate the build files
  },
});
